[package]
name    = "netmap-server"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = { version = "0.5", features = ["json", "tls"] }
serde      = { version = "1",   features = ["derive"] }
serde_json = "1"
uuid       = { version = "1",   features = ["v4"] }
